# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
# Note: Tests run locally and make HTTP requests to Docker container,
# so production dependencies are NOT needed here.

# Testing dependencies
sqlalchemy==2.0.49
pymysql==1.1.2
pytest==9.0.3
requests==2.33.1

# Code quality tools (optional but recommended)
pylint==4.0.5
black==26.3.1
mypy==1.20.1
